node_modules/

# Secrets — never commit real credentials
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
*.log

# OS/editor cruft
.DS_Store
Thumbs.db

# Ads and uploaded media (managed through the admin panel, not the repo)
server/data/
